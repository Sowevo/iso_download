# Linux发行版下载器使用说明

## 🚀 快速开始

### 1. 查看可用的发行版
```bash
python3 download_linux.py --list
```

### 2. 下载特定发行版
```bash
python3 download_linux.py --download "Ubuntu"
python3 download_linux.py --download "Deepin"
```

### 3. 下载所有发行版
```bash
python3 download_linux.py --download-all
```

### 4. 更新校验和

```bash
# 自动更新所有发行版的校验和
python3 download_linux.py --update-checksums
```

### 5. 指定下载目录

```bash
# 指定自定义下载目录
python3 download_linux.py --download "Ubuntu" --download-dir "/path/to/downloads"

# 默认下载到脚本所在目录的downloads文件夹
python3 download_linux.py --download "Ubuntu"
```

## ⚠️ 重要提示

### Python命令
- 在macOS上使用 `python3` 而不是 `python`
- 确保已安装Python 3.6或更高版本

### 依赖安装
```bash
pip3 install -r requirements.txt
```

## 🔍 过滤功能

### 按名称过滤
```bash
python3 download_linux.py --list --filter-name "Ubuntu"
```

### 按类型过滤
```bash
python3 download_linux.py --list --filter-type "linux"
```

## 📁 下载目录

文件将下载到 `downloads/` 目录下：
```
downloads/
├── Ubuntu/
├── Deepin/
├── CentOS/
├── Debian/
├── Fedora/
├── openSUSE/
├── Arch Linux/
└── Manjaro/
```

## 🌐 镜像源

所有发行版都使用阿里云镜像源，确保国内下载速度。

## 🔧 故障排除

### 常见问题
1. **命令未找到**: 使用 `python3` 而不是 `python`
2. **依赖缺失**: 运行 `pip3 install -r requirements.txt`
3. **网络问题**: 检查网络连接和镜像源可用性

### 获取帮助
```bash
python3 download_linux.py --help
```
